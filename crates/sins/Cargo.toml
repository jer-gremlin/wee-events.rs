[package]
name = "sins"
version = "0.0.0"
edition.workspace = true
publish = false

# Each binary demonstrates one claim from FEEDBACK.md by exercising the
# real wee-events public API. If anything here fails to compile or
# observably misbehave, the corresponding feedback item is wrong.

[dependencies]
wee-events = { path = "../wee-events" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
